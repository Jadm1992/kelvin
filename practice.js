
// I am assigning the variable kelvin to the expression 293
const kelvin = 293;
// here I use the let identifier and I'm assigning the variable celsius to the expression 273
const celsius = 273;
// next we console log the kelvin assign the subtraction assignment operator. Any value to the 
// right of the operand from a variable and assigns the result to the variable. 
console.log(kelvin - celsius)
// here we assign the let identifier to the variable fahrenheit and give it the value 33.8
// Fahrenheit = Celsius * (9/5) + 32
let fahrenheit = 33.8
// Math.round(33.8)
// next we want to round the number saved to fahrenheit. So in order to do that we use the 
// Math.round method from the built in Math object.
// console.log(Math.round(fahrenheit))
// here we use the .floor method to round down the fahrenheit temperature.
console.log(Math.floor(fahrenheit))
console.log(`The temperature is ${fahrenheit} degrees Fahrenheit.`)